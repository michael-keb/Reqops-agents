# Archive

Legacy code and data moved here during repo cleanup (2026-06). **Not used** by the active ReqOps + uplift-v6 + agent-sdk stack.

## Contents

| Path | What it was |
|------|-------------|
| `uplift-2.0/` | Early uplift gatekeeper prototype |
| `uplift-v3.0/` | v3 REPL + web + gatekeeper |
| `uplift-v4.0/` | v4 analyst / deterministic core |
| `uplift-v5/` | v5 headless-per-turn bridge (superseded by v6) |
| `uplift-v6-copy/` | Accidental duplicate of uplift-v6 |
| `uplift-2.0-empty/` | Empty placeholder folder |
| `legacy-root/` | Root scripts: `session_store.py`, `test-rubric.py`, `run-10-turns.sh`, etc. |
| `legacy-docs/` | Early rubric docs, gatekeeper plan, conversation notes |
| `legacy-sessions/` | Pre-v3 session artifacts from repo root `sessions/` |
| `runs/` | Empty legacy runs dir |

## Active stack (do not archive)

- `../agent-sdk/`
- `../agent-sdk-e2e/`
- `../uplift-v6/`
- `../uplift-reqops-e2e/`

Restore something from archive by moving it back to the repo root — no path references in active code point here.
