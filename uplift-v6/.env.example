# Uplift v6 bridge (ReqOps integration sidecar)
UPLIFT_PORT=8786
# headless = per-turn `agent` subprocess + stream-json (recommended for ReqOps)
# pty = interactive terminal UI
UPLIFT_AGENT_MODE=headless
# UPLIFT_MOCK_AGENT=1
